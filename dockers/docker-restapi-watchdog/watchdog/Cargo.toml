[package]
name = "restapi_watchdog"
version = "0.1.0"
edition = "2021"
description = "watchdog for the restapi container"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redis = "0.23.3"
