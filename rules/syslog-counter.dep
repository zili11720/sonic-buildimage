SPATH       := $($(SYSLOG_COUNTER)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/syslog-counter.mk rules/syslog-counter.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(SYSLOG_COUNTER)_CACHE_MODE  := GIT_CONTENT_SHA 
$(SYSLOG_COUNTER)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(SYSLOG_COUNTER)_DEP_FILES   := $(DEP_FILES)
