DPATH       := $($(DOCKER_RESTAPI_SIDECAR)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-restapi-sidecar.mk rules/docker-restapi-sidecar.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_RESTAPI_SIDECAR)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_RESTAPI_SIDECAR)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_RESTAPI_SIDECAR)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_RESTAPI_SIDECAR),$(DOCKER_RESTAPI_SIDECAR_DBG)))
