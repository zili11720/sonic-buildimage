[pytest]
# Test file patterns
python_files = test_*.py

# Test function patterns
python_functions = test_*

# Additional options
addopts =
    --verbose
    --strict-markers

# Python path for imports (includes fixtures directory)
pythonpath = test

# Test markers (optional - for categorizing tests)
markers =
    unit: Unit tests
    integration: Integration tests
